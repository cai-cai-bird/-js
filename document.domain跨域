document.domain 实现跨域 

这两个域名必须属于同一个基础域名!而且所用的协议，端口都要一致，否则无法利用document.domain进行跨域

例如:
aaa.xxx.com a.html
bbb.xxx.com b.html
若设置两个页面的document.domain 都为 xxx.com 两个页面就可以相互访问了  其它不可以

动态创建javascript 


window.name iframe 


jsonp 

postMessage
